[[annotation]]
file="$CONDA_PREFIX/data/problemsites.vcf.gz"
# ID and FILTER are special fields that pull the ID and FILTER columns from the VCF
fields = ["EXC", "FILTER"]
ops=["self", "self"]
names=["problem_exc", "problem_filter"]
